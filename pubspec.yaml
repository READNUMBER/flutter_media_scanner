name: media_scanner
description: A new flutter plugin project.
version: 0.0.1
author:

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.jukqaz.media_scanner
    pluginClass: MediaScannerPlugin
